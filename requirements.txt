numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0

matplotlib>=3.7
seaborn>=0.13

# PyTorch – dùng cho Entity Embeddings (CPU build mặc định).
# Nếu muốn GPU, cài lại theo hướng dẫn trên pytorch.org cho đúng bản CUDA.
torch>=2.2
